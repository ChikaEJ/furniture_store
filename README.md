# 📝 FastAPI Furniture app

## 📘 Описание проекта
**FastAPI Furniture app** — это backend-приложение для создания заказов мебели.  
Приложение реализовано на **FastAPI** с использованием **PostgreSQL**, **SQLAlchemy**, **Alembic**
Проект полностью контейнеризирован с помощью **Docker** и запускается одной командой.


---

## ⚙️ Конфигурация и база данных
Настройки проекта находятся в `core/config.py`.  
Подключение к базе данных выполняется через **SQLAlchemy**, а миграции управляются с помощью **Alembic**.

`core/database.py` содержит асинхронный движок и сессию SQLAlchemy.

---

## 🌐 API и маршруты

- `GET /furniture/` — список всей мебели.
- `GET /furniture/:id/` — информация о конкретном товаре.
- `POST /orders/` — создание заказа (принимает email клиента и список ID товаров, рассчитывает сумму).
- `GET /orders/` — список заказов по email клиента (query-параметр `email`).

---

### Возможности
- Просмотр мебели
- Просмотр всей мебели
- Просмотр заказа по email
- Создание заказа

---

## 🐳 Docker
Проект полностью контейнеризирован и готов к развертыванию.

### Сервисы
- **app** — FastAPI-приложение
- **db** — PostgreSQL
- **migration** — Alembic (автоматически применяет миграции при запуске)

### Запуск проекта
#### В терминале наберите команду 
```bash
git clone git@github.com:ChikaEJ/furniture_store.git
```
```bash
cd furniture_store
```
#### Разкоментируйте .env_example и введите данные вашей базы данных
#### Затем запустите докер команду для создания и запуска докер контейнера

```bash
docker-compose up --build
```
#### Запустите миргации командами ниже 

```bash
docker-compose exec app alembic upgrade head
```
После сборки приложение будет доступно по адресу:
```
http://localhost:8000/docs
```

---

## 🧠 Технологический стек
- **Python 3.11+**
- **FastAPI**
- **SQLAlchemy (async)**
- **Alembic**
- **PostgreSQL**
- **FastAPI Users**
- **Docker / Docker Compose**
- **Pydantic**

---

## 📦 Команды для разработки

### Запуск приложения локально
```bash
uvicorn app.main:app --reload
```

### Создание миграций Alembic
```bash
alembic revision --autogenerate -m "Init migration"
alembic upgrade head
```

### Остановка контейнеров
```bash
docker-compose down
```

---

## 🧾 Итог
**FastAPI Furniture App** — это готовое, модульное приложение.
